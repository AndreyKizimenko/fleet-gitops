controls:
  enable_disk_encryption: false
  ios_updates:
    deadline:
    minimum_version:
  ipados_updates:
    deadline:
    minimum_version:
  macos_migration:
    enable: false
    mode:
    webhook_url:
  macos_updates:
    deadline:
    minimum_version:
  scripts:
  windows_enabled_and_configured: true
  windows_migration_enabled: false
  windows_require_bitlocker_pin: false
  windows_updates:
    deadline_days:
    grace_period_days:
name: No team
policies:
- path: ../lib/macos/policies/bug-repro.policy.yml
software:
  packages:
  - path: ../lib/macos/packages/1password.package.yml
    self_service: true
    labels_include_any:
    - LabelToBeRemoved